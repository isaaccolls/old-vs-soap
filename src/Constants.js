const { innerWidth: width, innerHeight: height } = window;

const Constants = {
    MAX_WIDTH: width,
    MAX_HEIGHT: height,
    GAP_ZISE: 320,
    PIPE_WIDTH: 100,
    BIRD_WIDTH: 50,
    BIRD_HEIGHT: 41,
}

export default Constants;